.welcome-section {
  background: url(../images/content/backgrounds/bg1.png) center center / cover no-repeat;
}

.hero {
  height: 100vh;
  min-height: 650px;
}

.hero__container {
  display: flex;
  flex-direction: column;
  height: 100%;
}

.hero__header {
  @include tablets {
    margin-top: 60px;
  }
}

.hero__content {
  flex: 1;
  display: flex;
  justify-content: center;
  align-items: center;
  @include tablets {
    flex: 0.5;
  }
  @include phones {
    flex: 0.8;
  }
}

.hero__buttons {
  display: flex;
  justify-content: center;
  margin-bottom: 10%;
  @include tablets {
    display: none;
  }
}
