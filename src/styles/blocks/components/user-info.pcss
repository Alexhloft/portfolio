.user-info {
    display: flex;
}

.user-info__desc {
    font-size: 18px;
    font-weight: 600;
    line-height: 30px;
    text-align: right;
    color: $text;
}

.user-info__name {
    font-size: 72px;
    font-weight: 800;
    line-height: 60px;
    color: $white;
    margin-left: 60px;
}