.overlay {
    width: 100%;
    height: 100%;
    position: fixed;
    z-index: 50;
    background-color: $text;
    top: 0;
    left: 0;
    align-items: center;
    justify-content: center;
    display: none;
}

.overlay--active {
    opacity: 1;
    display: flex;
}